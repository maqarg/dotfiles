set number
set showmatch
set ignorecase
set hlsearch
set incsearch
set nowrap

" — Garantizar codificación UTF-8 para que reconozca la letra ñ
set encoding=utf-8

" — Mapear ñ a / en modo Normal y Visual
nnoremap ñ /
vnoremap ñ /
